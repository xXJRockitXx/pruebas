import 'package:flutter/material.dart';

class BackgroundServ extends StatefulWidget {
  const BackgroundServ({Key? key}) : super(key: key);

  @override
  State<BackgroundServ> createState() => _BackgroundServState();
}

class _BackgroundServState extends State<BackgroundServ> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
